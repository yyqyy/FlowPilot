"""FlowPilot local automation engine.

A headless engine that runs node-graph automation tasks against the real
desktop: find templates on screen and click/type at them, branch on whether an
image is present, launch apps, and wait. Tasks are triggered by a global hotkey
or run on demand through the local API.
"""
